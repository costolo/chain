<h1><%= current_user.name %></h1>

<div class="skills">
  <% current_user.skills.each do |skill| %>
    <%= skill.title + " - " + "Current streak: " + skill.current_streak.to_s + " - "%>
    <% if skill.time_remaining? %>
      Time remaining: 
      <%= content_tag(:span, :"data-countdown" => skill.expiration_date) {} %>
    <% else %>
      <%= skill.end_current_streak %>
    <% end %>
    <%= link_to('Refresh', refresh_path(skill)) %>
    <br>
  <% end %>
</div>

<%= form_for @new_skill do |f| %>
  <%= f.text_field :title %>
  <%= f.submit "add" %>
<% end %>

<script>
  addSkill();
  countdown();
</script>